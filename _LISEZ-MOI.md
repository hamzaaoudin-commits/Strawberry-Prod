# Strawberry — patch : retrait du plan d'architecture

Un seul fichier, `app/[lang]/page.tsx` — retiré de la home.

Supprimez aussi `components/strawberry/architecture-blueprint.tsx` du
dépôt : plus rien ne l'appelle, autant ne pas le laisser traîner.

## Fichiers inclus

- `app/[lang]/page.tsx`
